const initialState = {
    friends: [
        {id: 1, name: 'Larisa'},
        {id: 2, name: 'Dmitriy'},
        {id: 3, name: 'Alex'},
    ]
}

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer